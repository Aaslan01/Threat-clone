import {View, Text, ScrollView} from 'react-native';
import React from 'react';
import styles from '../assets/css/style';

const Search = () => {
  return (
    <ScrollView contentContainerStyle={styles.ContainerBlack}></ScrollView>
  );
};

export default Search;
